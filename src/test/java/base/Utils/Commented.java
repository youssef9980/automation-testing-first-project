package base.Utils;

public class Commented {
    // driver.navigate().to("http://www.google.com");
    //driver.navigate().back();
    ///Thread.sleep(2000);
    //driver.navigate().refresh();
    //String title = driver.getTitle();
    // System.out.println(title);
    //driver.quit(); // close all windows tabs
    //driver.close(); // close one tab
    //driver.findElement(By.id("login-button"));
    //driver.findElement(By.name("login-button"));
    //Xpath:
        /*
           1-//tagName[@attribute = 'value of attribute']
           2-
         */


    // CSS selector
        /*
        1- #ID
        2- .className
        3. [data-testid = value]
        4. [name = value]
        5. .parentClass .childClass -> if we need to find child class from its parent
       */
    //driver.findElements(By.cssSelector(".parentClass childclasses")).get(5);

    // Thread.sleep(1000);
    //driver.quit();
}
